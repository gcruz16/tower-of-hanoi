console.log("scrit linked");



let disksArray = document.querySelectorAll(".disk-item");

disksArray.forEach(disk => {
    // let diskArray = disk;
    console.log(disk.id);
    // diskArray.forEach(diskItem => {
    //     console.log(diskItem);
    // });

});